#ifndef MAP_H_
#define MAP_H_

#include <cstddef>
#include <vector>

class Map {
    std::vector<>
    public:
        const std::size_t size_x,
                        size_y;
        Map(std::size_t size_x, std::size_t size_y) : size_x(size_x), size_y(size_y)
        {

        }
}


#endif // MAP_H_
